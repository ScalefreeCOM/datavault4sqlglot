id: "2.1.2.1"
title: "Incremental, one source, one BK: existing BK in incoming batch -> no inserts"
entity: hub
mode: incremental

entity_spec:
  target: { database: DV, schema: RAW_VAULT, table: HUB_ORDER }
  hashkey: HK_ORDER_H
  business_keys: [ORDER_ID]
  sources:
    - { database: RAW_DB, schema: STAGE, table: STG_ORDERS,
        load_date_col: LOAD_DATE, record_source_col: RECORD_SOURCE }

current_state:
  - table: DV.RAW_VAULT.HUB_ORDER
    rows:
      - { HK_ORDER_H: "h1", ORDER_ID: "A1", ldts: "2026-01-01", rsrc: "ERP/ORDERS" }

input:
  - table: RAW_DB.STAGE.STG_ORDERS
    rows:
      - { HK_ORDER_H: "h1", ORDER_ID: "A1", LOAD_DATE: "2026-01-05", RECORD_SOURCE: "ERP/ORDERS" }

expect:
  match_mode: empty
