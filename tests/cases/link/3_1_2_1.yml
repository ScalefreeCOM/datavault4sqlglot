id: "3.1.2.1"
title: "Incremental, one source: existing link HK in incoming batch -> no inserts"
entity: link
mode: incremental

entity_spec:
  target: { database: DV, schema: RAW_VAULT, table: LNK_ORDER_CUSTOMER }
  link_hash_key: HK_LNK_ORDER_CUSTOMER_L
  foreign_hash_keys: [HK_ORDER_H, HK_CUSTOMER_H]
  sources:
    - { database: RAW_DB, schema: STAGE, table: STG_ORDERS,
        load_date_col: LOAD_DATE, record_source_col: RECORD_SOURCE }

current_state:
  - table: DV.RAW_VAULT.LNK_ORDER_CUSTOMER
    rows:
      - { HK_LNK_ORDER_CUSTOMER_L: "lnk1", HK_ORDER_H: "ord_a", HK_CUSTOMER_H: "cust_a", ldts: "2026-01-01", rsrc: "ERP/ORDERS" }

input:
  - table: RAW_DB.STAGE.STG_ORDERS
    rows:
      - { HK_LNK_ORDER_CUSTOMER_L: "lnk1", HK_ORDER_H: "ord_a", HK_CUSTOMER_H: "cust_a", LOAD_DATE: "2026-01-05", RECORD_SOURCE: "ERP/ORDERS" }

expect:
  match_mode: empty
